name: Validate skill links

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate-skill-links:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Validate local links in SKILL.md files
        run: python3 scripts/validate_skill_links.py
